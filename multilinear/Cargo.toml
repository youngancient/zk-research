[package]
name = "multilinear"
version = "0.1.0"
edition = "2021"

[dependencies]
ark-ff = "0.5.0"
ark-bn254 = "0.5.0"

[dev-dependencies]
criterion = { version = "0.4", features = ["html_reports"] }

[[bench]]
name = "benchmark"
harness=false